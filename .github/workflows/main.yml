name: Tomas-Proj

on: [push]
jobs:
  test:
   runs-on: ubuntu-20.04

   steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Run tests
      uses: cypress-io/github-action@v2

      with:
        command: npx cypress run --record --key 6da2397c-604f-497b-a44c-3b6ce125e08c
        start: npm start
        record: true
        


